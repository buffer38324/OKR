<Page
    x:Class="Okr.TodoItemPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Okr"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <ScrollViewer>
    <Grid Background="#D3E7EE" x:Name="TodoItemGrid" Loaded="TodoItemGrid_Loaded">
        <Grid.RowDefinitions>
            <RowDefinition Height="90"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Margin="0,30,0,0" Text="今天也是元气满满的一天哦" CharacterSpacing="100" FontSize="28px" IsColorFontEnabled="True" HorizontalAlignment="Center"></TextBlock>
            <StackPanel  Margin="10,5,0,0" x:Name="ItemList" Grid.Row="1" 
                   HorizontalAlignment="Center" 
                   >
            </StackPanel>
    </Grid>
    </ScrollViewer>
</Page>
